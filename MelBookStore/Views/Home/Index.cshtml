@{
    ViewData["Title"] = "Home Page";
}

@model ProjectListViewModel

<!-- goes through each partial view and displays the results-->

@foreach (var x in Model.Projects)
{
    <partial name="ProjectSummary" model="x" />
}

